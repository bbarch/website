#!/usr/bin/env bash
# All site images are now bundled locally in this folder — no download needed.
# This script is kept only as a placeholder and can be safely deleted.
echo "Images are already local in the images/ folder. Nothing to download."
